<%= render 'shared/sidebar' %>

<div class="mx-auto max-w-2xl px-6 py-10">
  <%= link_to "← #{@client.name}", onboarding_client_path(@client),
        class: "font-[JetBrains_Mono] text-[11px] uppercase tracking-[0.14em] text-[#7620B1] hover:underline" %>

  <h1 class="mt-3 font-[Outfit] text-2xl font-semibold text-[#150D3A]">Edit client</h1>

  <div class="mt-7 rounded-2xl bg-white p-6 ring-1 ring-[#150D3A]/10">
    <%= render "form", client: @client, submit_label: "Save changes" %>
  </div>
</div>
